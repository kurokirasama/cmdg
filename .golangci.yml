version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters-settings:
  staticcheck:
    checks:
      - all
      - -ST1005
      - -SA1019
      - -QF1004
      - -QF1001
